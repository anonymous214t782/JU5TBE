let featuredPost = ["Software Engineer,  Trader,  Philosopher"]
//arrays are 0 indexed num start at 0
//arrays are composite/complex data type(can store any data type)
//strings are primitive

let atoms = ["Atom", 19, true]
atoms.push("okay")//push func add to array
console.log(atoms)

let cards = [7, 4]
cards.pop([1])//removes array (which is 4 in this case
console.log(cards)


let messages = [
   "Hey hows it going?",
   "Im good and you",
   "Im good,What do you think about absurdism?"
]
yes = "Yes ('_')"
messages.push(yes)
console.log(messages[3])
